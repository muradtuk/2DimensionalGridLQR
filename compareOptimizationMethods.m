function [outputArg1,outputArg2] = compareOptimizationMethods(n_rows, n_cols, d)
    
    R = createSymbolicMatrix('R', 1, 0, d, 1, 0);
    N = createSymbolicMatrix('N', 1, 0,  d, 1, 0);
    Q = createSymbolicMatrix('Q', 1, 0, d, 1, 0);
    A = createSymbolicMatrix('A', 1, 0, d, 1, 0);
    B = createSymbolicMatrix('B', 1, 0, d, 1, 0);
    C = createSymbolicMatrix('C', 1, 0, d, 1, 0);
    D = createSymbolicMatrix('D', 1, 0, d, 1, 0);
    
    
    
    
end

